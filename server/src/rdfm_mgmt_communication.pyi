from request_models import *
import socket
from _typeshed import Incomplete
from typing import Optional

HEADER_LENGTH: int

class Client:
    name: Incomplete
    def __init__(self, name: str, socket: Optional[socket.socket]) -> None: ...
    def get_server_addr(self) -> tuple[str, int]: ...
    def send(self, message: Request) -> None: ...
    def receive(self) -> Optional[Request]: ...
    def set_socket(self, s: socket.socket): ...

class Device(Client):
    required_capabilities: dict[str, list[str]]
    mac_address: Incomplete
    capabilities: Incomplete
    metadata: Incomplete
    def __init__(self, name: str, socket: Optional[socket.socket], mac_address: str, capabilities: dict) -> None: ...
    def can_handle_request(self, request_method: str) -> bool: ...
    def set_socket(self, s: socket.socket) -> None: ...

class User(Client):
    def __init__(self, name: str, socket: socket.socket) -> None: ...
    def prompt(self, message: str) -> None: ...

class FileTransfer:
    receiver: Incomplete
    sender: Incomplete
    file_path: Incomplete
    def __init__(self, receiver: Client, sender: Optional[Client], file_path: str) -> None: ...

def receive(client: socket.socket) -> Optional[Request]: ...
def encode_json(to_encode: Request) -> bytes: ...
def decode_json(to_decode: bytes) -> Request | int: ...
def create_client(client_group: ClientGroups, name: str, socket: socket.socket, capabilities: Optional[dict]) -> Optional[Client]: ...
def create_listening_socket(hostname: str, port: int = ..., encrypted: bool = ..., crt: str = ..., key: str = ...) -> socket.socket: ...
